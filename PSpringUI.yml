spring:
  application:
    name: PSpringUI

management:
  endpoints:
    web:
      exposure:
        include=*:

server:
  port: ${PORT:8080}


---
spring:
  profiles: cloud

uri:
  registry: http://PSPRINGAPI
  gateway: https://gateway-6cff540c-50e4-4249-a756-db956437f153.apps.cf.martin-bw-piv.net/api
