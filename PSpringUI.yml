spring:
  application:
    name: PSpringUI

management:
  endpoints:
    web:
      exposure:
        include=*:

server:
  port: ${PORT:8080}


---
spring:
  profiles: cloud

uri:
  registry: http://PSPRINGAPI
  gateway: https://gateway-863ed08c-53db-405c-831f-2f1675dde9e6.apps.cf.martin-bw-piv.net/api/1
